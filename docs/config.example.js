// Copy this to docs/config.js and set your publishable (pk.*) Mapbox token to
// preview the map locally. docs/config.js is gitignored; the GitHub Pages deploy
// workflow writes it from the MAPBOX_TOKEN Actions secret at build time, so the
// token never lives in the repo. Use a URL-restricted publishable token — never
// a secret sk.* token.
window.CPAL_MAPBOX_TOKEN = "pk.your-publishable-mapbox-token";
